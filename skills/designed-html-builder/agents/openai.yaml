interface:
  display_name: "Designed HTML Builder"
  short_description: "Executable HTML design workflow with runtime audits"
  default_prompt: "Design or improve this website, HTML page, resume, or DESIGN.md by first running the design-context loader, emitting DHB_PREFLIGHT, using Impeccable and DESIGN.md runtime checks when available, falling back honestly to vendored references when unavailable, and running visual/PDF QA as needed."
